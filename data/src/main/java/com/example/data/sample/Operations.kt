package com.example.data.sample

object Operations {
   fun add(x: Int, y: Int): Int = x + y

   fun subtract(x: Int, y: Int): Int {
      return x - y
   }

   fun domeSomeMoreWork() {

   }
   fun multiply(x: Int, y: Int): Int = x * y
   fun divide(x: Int, y: Int): Int = x / y
}